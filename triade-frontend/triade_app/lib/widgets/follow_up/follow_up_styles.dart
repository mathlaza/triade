import 'package:flutter/material.dart';

/// Cores e estilos compartilhados para o Follow-up Screen
/// NOTA: Não altere esses valores - são os mesmos do design original

// Cores base
const Color followUpBackgroundColor = Color(0xFF000000);
const Color followUpSurfaceColor = Color(0xFF1C1C1E);
const Color followUpCardColor = Color(0xFF2C2C2E);
const Color followUpBorderColor = Color(0xFF38383A);

// Cores de texto
const Color followUpTextPrimary = Color(0xFFFFFFFF);
const Color followUpTextSecondary = Color(0xFF8E8E93);

// Cores de destaque
const Color followUpAccentGold = Color(0xFFFFD60A);
const Color followUpAccentOrange = Color(0xFFFF9F0A);

// Cores de status
const Color followUpSuccessGreen = Color(0xFF30D158);
const Color followUpWarningYellow = Color(0xFFFFD60A);
const Color followUpErrorRed = Color(0xFFFF453A);
